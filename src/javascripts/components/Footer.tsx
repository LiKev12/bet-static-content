import { Grid } from '@mui/material';

const Footer: React.FC = () => {
    return (
        <footer
            style={{
                width: '100%',
            }}
        >
            <Grid container>
                <Grid item>Home</Grid>
            </Grid>
        </footer>
    );
};

export default Footer;
